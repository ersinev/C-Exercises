using System;

namespace _04_CurzonNumbers
{
    class Program
    {
        static void Main(string[] args)
        {
            int number = Convert.ToInt32(Console.ReadLine()) + 1;
            double powerterm, productterm, result;
            bool sonuc = true;
            while (sonuc)
            {
                Console.WriteLine("icerde");
                //powerterm = Math.Pow(2, number) + 1;
                //productterm = (2 * number) + 1;
                //result = powerterm % productterm;

                //if (result==0)
                //{
                //    sonuc = false;
                //    Console.WriteLine(number);
                //}

                //number = number + 1;
            }
            Console.WriteLine("adasdas");
            for (int i = 0; i < 2; i++)
            {
                for (int j = 0; j < 2; j++)
                {
                    Console.WriteLine();
                }
            }
        }
    }
}
